config:
  backup: true
  create: true
  dotpath: dotfiles
dotfiles:
  f_starship.toml:
    dst: ~/.config/starship.toml
    src: config/starship.toml
profiles:
  general:
    dotfiles:
    - f_starship.toml
